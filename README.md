# CodeAlpha_Music-Generation-with-AI
This project uses AI and LSTM neural networks to generate music from MIDI files. The model learns musical note patterns from uploaded datasets, predicts new note sequences, and converts them into playable MIDI music files using Python, TensorFlow, and music21 libraries.
